import React from "react";
import ImageList from '@mui/material/ImageList'

const List = (props) => {

    return <div>
        <ImageList cols={3} rowHeight={165}>
            
        </ImageList>
    </div>
}

export default List